@page
@model Greenfield.Web.Pages.Reservations.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<div class="container p-3 bg-light bg-opacity-50">
    <h1>Delete</h1>
    <hr />
    <h3>Are you sure you want to delete this reservation?</h3>
    <div>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Reservation.Property)</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.Reservation.Property.Name)</dd>
            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Reservation.Date)</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.Reservation.Date)</dd>
        </dl>

        <form method="post">
            <input type="hidden" asp-for="Reservation.Id" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a asp-page="./Index">Back to List</a>
        </form>
    </div>
</div>
